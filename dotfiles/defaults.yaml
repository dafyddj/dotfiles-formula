# -*- coding: utf-8 -*-
# vim: ft=yaml
---
dotfiles:
  pkg:
    name: dotfiles
  rootgroup: root
  config: '/etc/dotfiles'
  service:
    name: dotfiles
  subcomponent:
    config: '/etc/dotfiles-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
